<Window x:Class="PL.OrderWindows.OrderUpdateWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL.OrderWindows"
        xmlns:vsibility="clr-namespace:PL.vsibility"
        
       
        mc:Ignorable="d"
        DataContext="{Binding RelativeSource={RelativeSource self}}"
        Title="OrderUpdateWindow" Height="450" Width="800">

    <Window.Resources>

        <Style TargetType="Label">
            <Setter Property="Background" Value="{x:Null}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="15" />
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="HorizontalContentAlignment" Value = "Center"/>
        </Style>

        <Style TargetType="TextBlock">
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="TextAlignment" Value="Center"/>
            <Setter Property="FontWeight" Value="Bold"/>

        </Style>

    </Window.Resources>

    <Grid>
        <Grid.Background>
            <ImageBrush ImageSource="/pics/red-wine.jpg"/>
        </Grid.Background>

        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>

        </Grid.RowDefinitions>

        <Grid Grid.Row="0">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*" />

            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Label Content="Customer Name"/>
            <TextBlock Text="{Binding Order1.CustomerName}"  />

            <Label Grid.Row="1" Content="Customer Address"/>
            <TextBlock Grid.Row="1" Text="{Binding Order1.CustomerAddress}"  />


            <Label Grid.Row="2" Content="Customer Email"/>
            <TextBlock Grid.Row="2" Text="{Binding Order1.CustomerEmail}"  />


            <Label  Grid.Column="1" Grid.Row="0" Content="Status"/>
            <TextBlock  Grid.Column="1" Grid.Row="0"  Text="{Binding Order1.Status}"/>

            <Label  Grid.Column="1" Grid.Row="1" Content="ID"/>
            <TextBlock Grid.Column="1" Grid.Row="1"  Text="{Binding Order1.ID}"/>

            <Label  Grid.Column="1" Grid.Row="2" Content="Total Price"/>
            <TextBlock Grid.Column="1" Grid.Row="2"  Text="{Binding Order1.TotalPrice}"/>

            <Label  Grid.Column="2" Grid.Row="0" Content="Order Date"/>
            <TextBlock Grid.Column="2" Grid.Row="0"  Text="{Binding Order1.OrderDate}"/>

            <Label  Grid.Column="2" Grid.Row="1" Content="Ship Date"  />
            <TextBlock Grid.Column="2" Grid.Row="1"  Text="{Binding Order1.ShipDate}"/>

            <Label  Grid.Column="2" Grid.Row="2" Content="Delivery Date" />
            <TextBlock Grid.Column="2" Grid.Row="2"  Text="{Binding Order1.DeliveryDate}"/>

        </Grid>

        <Button Content="Back" Grid.Row="1" Margin="10,172,740,15"  Style="{StaticResource ButtonStyleLowerRuler}"  />

        <Button Grid.Row="1" Content="Update Ship" Style="{StaticResource ButtonStyle1}" Margin="10,24,650,145" FontSize="18" Visibility="{Binding Converter={StaticResource vsibility}}" />
        

        <ListView ItemsSource="{Binding Order1.ListOfItems}" Foreground="White" FontFamily="Calibri" FontSize="18"  FontWeight="Bold" BorderBrush="{x:Null}" Background="{x:Null}" VerticalAlignment="Top" HorizontalAlignment="Left" Width="464" Height="197" Grid.Row="1" Margin="309,5,0,0">

            <ListView.View>
                <GridView>
                    <GridViewColumn DisplayMemberBinding ="{Binding ProductName}" Width="auto">
                        <GridViewColumn.Header>
                            <GridViewColumnHeader >Name</GridViewColumnHeader>
                        </GridViewColumn.Header>
                    </GridViewColumn>
                    <GridViewColumn DisplayMemberBinding ="{Binding ProductID}" Width="auto">
                        <GridViewColumn.Header>
                            <GridViewColumnHeader >ID</GridViewColumnHeader>
                        </GridViewColumn.Header>
                    </GridViewColumn>

                    <GridViewColumn DisplayMemberBinding ="{Binding Price}" Width="auto">
                        <GridViewColumn.Header>
                            <GridViewColumnHeader  >Price</GridViewColumnHeader>
                        </GridViewColumn.Header>
                    </GridViewColumn>

                    <GridViewColumn DisplayMemberBinding ="{Binding Amount}" Width="auto">
                        <GridViewColumn.Header>
                            <GridViewColumnHeader >Amount</GridViewColumnHeader>
                        </GridViewColumn.Header>
                    </GridViewColumn>

                    <GridViewColumn DisplayMemberBinding ="{Binding TotalPrice}" Width="auto">
                        <GridViewColumn.Header>
                            <GridViewColumnHeader Tag="TotalPrice" >Total Price</GridViewColumnHeader>
                        </GridViewColumn.Header>
                    </GridViewColumn>
                </GridView>
            </ListView.View>

            <ListView.Resources>
                <Style TargetType="{x:Type ListViewItem}">
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#FF853636"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </ListView.Resources>

        </ListView>




    </Grid>
</Window>
